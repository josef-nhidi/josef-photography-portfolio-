# =============================================================================
# Josef Photography — Production Nginx Configuration
# =============================================================================
# This config serves the React SPA and proxies /api + /storage to Laravel.

upstream laravel {
    server app:80;
}

server {
    listen 80;
    server_name _;

    # --- Max upload size (for high-res photography uploads) ---
    client_max_body_size 64M;

    # --- Logging ---
    access_log /var/log/nginx/access.log;
    error_log  /var/log/nginx/error.log;

    # --- Frontend (React SPA) ---
    root /usr/share/nginx/html;
    index index.html;

    # --- Gzip compression for faster delivery ---
    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript image/svg+xml;

    # --- Static asset caching (JS, CSS, images) ---
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|webp|woff|woff2|ttf|eot)$ {
        expires 30d;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
    }

    # --- API proxy to Laravel ---
    location /api {
        proxy_pass http://laravel;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 300;
        proxy_connect_timeout 300;
    }

    # --- Storage proxy (uploaded photos served by Laravel) ---
    location /storage {
        proxy_pass http://laravel;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # --- Laravel Sanctum / auth cookies ---
    location /sanctum {
        proxy_pass http://laravel;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # --- React SPA fallback (all other routes go to index.html) ---
    location / {
        try_files $uri $uri/ /index.html;
    }
}
